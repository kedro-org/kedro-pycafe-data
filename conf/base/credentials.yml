snowflake_credentials:
  account: ${oc.env:SNOWFLAKE_ACCOUNT,null}
  role: ${oc.env:SNOWFLAKE_ROLE,null}
  user: ${oc.env:SNOWFLAKE_USER,null}
  password: ${oc.env:SNOWFLAKE_PASSWORD,null}
  warehouse: "KEDRO_BI_WH_WH"
  database: "KEDRO_BI_DB"
  schema: "PYPI"

heap_snowflake_credentials:
  account: ${oc.env:SNOWFLAKE_ACCOUNT,null}
  user: ${oc.env:SNOWFLAKE_USER,null}
  password: ${oc.env:SNOWFLAKE_PASSWORD,null}
  role: HEAP_NTD_KEDRO
  warehouse: HEAP_NTD_KEDRO_WH
  database: "HEAP_FRAMEWORK_VIZ_PRODUCTION"
  schema: "HEAP"

